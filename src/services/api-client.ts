import { getIngredients } from "./ingredients";

import * as products from "./findProducts";
import {
  getPizzas,
  getBreakfast,
  getCocktails,
  getDrinks,
  getSnacks,
} from "./products";

export const Api = {
  products,
  getIngredients,
  getPizzas,
  getBreakfast,
  getCocktails,
  getDrinks,
  getSnacks,
};
